# TRD §6 — Python + FastAPI for the AI/voice layer: LLM orchestration, STT/TTS and audio
# processing. Pinned to minor versions so a fresh clone builds the same thing.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
redis==5.2.1
python-dotenv==1.0.1

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
